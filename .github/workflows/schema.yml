name: test 

on: push

jobs:
  automation_test:
    runs-on: ubuntu-latest
    steps:
      - name: generate_json
        run: |
          pixi run generate-schema
      - name: generate_md
        run: |
          pixi run tomarkdown
      - name: extend_reference
        run: 
    #run generate schema from model.py > schema.json: pixi run generate-schema
    #run schema.json > index.md: pixi run tomarkdown
    #run extend mkdocs reference based on index.md
